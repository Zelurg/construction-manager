# Database configuration
DATABASE_URL=postgresql://user:password@localhost:5432/construction_db

# CORS origins (для локальной сети добавьте IP адреса)
# Пример: CORS_ORIGINS=http://192.168.1.100:3000,http://192.168.1.101:3000
CORS_ORIGINS=http://localhost:3000,http://localhost:5173

# Для разработки в локальной сети можно временно разрешить все источники
# ВНИМАНИЕ: не используйте это в продакшене!
# ALLOW_ALL_ORIGINS=true
