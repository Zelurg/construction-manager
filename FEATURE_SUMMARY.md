# Реализация справочника сотрудников и учета исполнителей

## Описание

Добавлена новая фунциональность:
- Справочник сотрудников (новая вкладка)
- Указание исполнителей за каждый день (в Ежедневных нарядах)
- Анализ эффективности (сравнение отработанных часов с трудозатратами)

## Изменения в Backend

### Новые файлы:
1. `backend/app/models.py` - добавлены модели Employee и DailyExecutor
2. `backend/app/schemas.py` - добавлены схемы Pydantic
3. `backend/migrations/add_employees.sql` - SQL миграция
4. `backend/apply_employees_migration.py` - скрипт применения миграции
5. `backend/app/routes/employees.py` - API роуты для сотрудников
6. `backend/app/routes/executors.py` - API роуты для исполнителей
7. `backend/EMPLOYEES_README.md` - документация

### Обновленные файлы:
1. `backend/app/routes/__init__.py` - регистрация новых роутов

## Изменения в Frontend

### Новые файлы:
1. `frontend/src/components/Directories.js` - главный компонент Справочники
2. `frontend/src/components/Employees.js` - управление сотрудниками
3. `frontend/src/styles/Directories.css` - стили Справочников
4. `frontend/src/styles/Employees.css` - стили сотрудников

### Обновленные файлы:
1. `frontend/src/services/api.js` - добавлены employeesAPI и executorsAPI
2. `frontend/src/components/DailyOrders.js` - добавлена работа с исполнителями
3. `frontend/src/App.js` - добавлена вкладка Справочники
4. `frontend/src/styles/DailyOrders.css` - добавлены стили для исполнителей

## Инструкция по установке

### 1. Примените миграцию:

```bash
cd backend
python apply_employees_migration.py
```

### 2. Перезапустите backend:

```bash
uvicorn app.main:app --reload
```

### 3. Перезапустите frontend:

```bash
cd frontend
npm start
```

## Как использовать

### Добавление сотрудников:
1. Перейдите на вкладку "Справочники" → "Сотрудники"
2. Нажмите "+ Добавить сотрудника"
3. Заполните ФИО и профессию
4. Сохраните

### Указание исполнителей за день:
1. Перейдите на вкладку "Ежедневные наряды"
2. Выберите дату
3. Нажмите "👥 Указать исполнителей"
4. Выберите ответственного (прораба)
5. Отметьте галочками исполнителей и укажите часы
6. Сохраните

### Просмотр статистики:
После указания исполнителей над таблицей появится информация:
- Количество исполнителей
- Отработанные часы
- Трудозатраты по нормам
- Ответственный

## Особенности

### Бизнес-логика:
- На один день может быть только один ответственный
- Ответственный не может быть одновременно исполнителем
- Нельзя добавить одного сотрудника дважды на один день

### Эффективность:
Система сравнивает отработанные часы с трудозатратами:
- 🟢 Зеленый - разница меньше 1 часа (нормально)
- 🟠 Оранжевый - отработано больше чем по нормам (низкая эффективность)
- 🔵 Синий - отработано меньше чем по нормам (высокая эффективность)

## API Endpoints

### Сотрудники:
- `GET /api/employees/` - получить список
- `GET /api/employees/{id}` - получить по ID
- `POST /api/employees/` - создать
- `PUT /api/employees/{id}` - обновить
- `DELETE /api/employees/{id}` - удалить
- `PATCH /api/employees/{id}/deactivate` - деактивировать
- `PATCH /api/employees/{id}/activate` - активировать

### Исполнители:
- `GET /api/executors/?work_date=YYYY-MM-DD` - получить за дату
- `GET /api/executors/stats?work_date=YYYY-MM-DD` - статистика
- `POST /api/executors/` - добавить
- `PUT /api/executors/{id}` - обновить
- `DELETE /api/executors/{id}` - удалить

## WebSocket события

- `executor_added` - добавлен исполнитель
- `executor_updated` - обновлен исполнитель
- `executor_deleted` - удален исполнитель
